import 'package:build/build.dart';

import 'src/super_annotations_builder.dart';

/// Entry point for the builder
SuperAnnotationsBuilder buildSuperGeneration(BuilderOptions options) =>
    SuperAnnotationsBuilder(options);
